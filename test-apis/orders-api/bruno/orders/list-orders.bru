meta {
  name: List My Orders
  type: http
  seq: 1
}

get {
  url: {{base_url}}/orders
  body: none
  auth: bearer
}

auth:bearer {
  token: {{alice_token}}
}

assert {
  res.status: eq 200
  res.body.orders: isArray
}
